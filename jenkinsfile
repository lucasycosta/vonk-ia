pipeline {
    agent any

    stages {
        stage('Build and Run Docker Compose') {
            steps {
                script {
                    // Construir e rodar o docker-compose
                    bat 'docker-compose up -d' // Construir e rodar no modo detached
                }
            }
        }
    }
}
